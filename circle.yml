---
test:
  override:
    - coverage run manage.py test
deployment:
  production:
    branch: master
    commands:
      - git remote add dokku dokku@dtelab.com.ar:testing-suite-admin
      - git push dokku master:master
